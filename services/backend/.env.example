# .env.example - copy to .env and edit
# Fernet key for symmetric encryption (development only). Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=your_fernet_key_here
# Simple admin API key to protect endpoints (development only). Use a strong value.
ADMIN_API_KEY=change_me_to_a_long_random_string
# DB url (for production use Postgres). Default uses sqlite file 'backend.db' in project root.
DATABASE_URL=sqlite:///./backend.db
